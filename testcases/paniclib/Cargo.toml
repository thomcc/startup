[package]
name = "paniclib"
version = "0.1.0"
authors = ["Thom Chiovoloni <chiovolonit@gmail.com>"]
edition = "2018"
publish = false

[lib]
name = "paniclib"
crate_type = ["cdylib"]
path = "lib.rs"

[dependencies]
startup = { path = "../.." }
